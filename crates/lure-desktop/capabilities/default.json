{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lure 桌面默认权限：仅 core 默认能力（外壳加载 loopback WebUI，不额外开放 IPC）。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
